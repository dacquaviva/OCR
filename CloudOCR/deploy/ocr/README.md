# inference_ocr

This repository is intended to interact with triton server for ocr, using a flask server as interface.

## Usage

### Docker Compose
Simply clone the repository, then:

### Build
```shell
$ docker compose -f docker-compose.dev.yaml build
```
### Run
```shell
$ docker compose -f docker-compose.dev.yaml up
```